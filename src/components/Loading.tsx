import { MrMiyagi } from "@uiball/loaders";

const Loading = () => {
  return (
    <div className="flex justify-center items-center w-screen h-screen">
      <MrMiyagi size={35} lineWeight={3.5} speed={1} color="#0077b5" />
    </div>
  );
};

export default Loading;
